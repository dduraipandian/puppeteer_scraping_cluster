"use strict";

const APP_PORT = Number(process.env.PORT);

module.exports = {
    APP_ROOT,
    APP_PORT,
    APP_NAME,
    LOGLEVEL,
    REDIS_URL,
    SYNC_TIMER,
    PRIVATE_IP,
    PROXY_PING_EXPIRY
}